package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Lite "image_9"-inspired elegant pastel color palette
val LiteBlue = Color(0xFFA2C2FC)       // Safe, modern Soft Lite Blue (Positive/Earning)
val LiteCyan = Color(0xFF9BF2EC)       // Soft Lite Cyan/Teal (Neutral)
val LiteGreen = Color(0xFFBBECC4)      // Soft Lite Green (Remaining)
val LitePeach = Color(0xFFFFECEF)      // Very soft, light pastel peach/pink for withdrawal highlights
val LiteRed = Color(0xFFFFC6C6)        // Extremely soft pastel red
val LiteOrange = Color(0xFFFFDAB9)     // Soft Peach-Orange
val LiteYellow = Color(0xFFFFF2AF)     // Soft light pastel yellow
val LiteLavender = Color(0xFFE1D5EC)   // Soft light lavender-purple

// Airy UI Structural Colors (Light & Breezy)
val MinimalPrimary = Color(0xFF4376F7)       // Airy Soft Blue
val MinimalSecondary = Color(0xFF6B7A99)     // Airy muted slate-blue
val MinimalTertiary = Color(0xFFE9F0FE)      // Very soft blue selection pill background
val MinimalOnTertiary = Color(0xFF0D256B)    // Dark slate query text for options
val MinimalBackground = Color(0xFFEBF1F8) 
val MinimalSurface = Color(0xFFE6EDF5) 
val MinimalOnBackground = Color(0xFF1E2638)  // Balanced dark navy-slate body text
val MinimalOnSurface = Color(0xFF1E2638)     // Balanced dark navy-slate card text
val MinimalBorder = Color(0xFFD3DFEF)        // Super soft, thin background separator borders
val MinimalSurfaceVariant = Color(0xFFD8E2ED) // Airy container variant, used for bottom nav

// Minimal Highlight / Category Pastel Card Palette
val EarningCardBg = Color(0xFFD0E6FA)        // Deeper soft blue-sky background
val EarningCardText = Color(0xFF033B6C)      // Matching dark contrast text
val ExpenseCardBg = Color(0xFFF6E1E4)        // Slightly deeper delicate peach/orange
val ExpenseCardText = Color(0xFF751A1F)      // Matching deep berry-wine text
val SavingsCardBg = Color(0xFFD4EDDB)        // Delicate deeper mint
val SavingsCardText = Color(0xFF0F4D1B)      // Solid dark forest green

val BalanceCardBg = Color(0xFFD8E6F8)        // Clean airy steel-blue
val BalanceCardText = Color(0xFF0F2244)      // Super deep navy slate
val BalanceCardHighlight = Color(0xFF4376F7)  // Highlights are styled in Soft Blue

// Maintain compatibility references (points to cohesive slate-green/blue minimal theme)
val EmeraldPrimary = MinimalPrimary
val EmeraldSecondary = MinimalSecondary
val EmeraldTertiary = MinimalTertiary

val EarningGreen = Color(0xFF4DA658)        // Desaturated, friendly semantic green
val ExpenseRed = Color(0xFFE57373)          // Friendly desaturated semantic red
val SavingsBlue = Color(0xFF64B5F6)         // Soft pastel blue
val SavingsWithdrawalPink = Color(0xFFF06292) // Soft pastel pink
